# Fixed benchmark prompts

These prompts are the controlled inputs for Modules 0–4 in `COMPARATIVE_BENCHMARK.md`. Do not edit a prompt during an active comparison. Version protocol changes separately, reset every model to `baseline-v1`, and record the prompt file names in each run's metadata.

Each module is autonomous: the model may inspect, edit, test, and summarize, but receives no additional implementation guidance after the prompt is issued.
